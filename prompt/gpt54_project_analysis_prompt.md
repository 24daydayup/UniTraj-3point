# GPT-5.4 提示词：分析修改后的项目并生成 Markdown 文档

请你作为一名资深代码架构分析助手，**完整阅读并分析当前项目代码**，重点你修改后的版本，并输出一份**结构化、可直接保存为 `.md` 文件的项目说明文档**。

## 任务目标

请基于当前项目的实际代码，生成一份详细的 Markdown 文档，内容需要覆盖：

1. **项目整体架构**
   - 项目的总体目标
   - 整体数据流 / 模型流 / 训练流 / 推理流
   - 目录结构与模块之间的依赖关系
   - 原始 UniTraj 主干与我新增/修改部分的关系

2. **各个模块的功能说明**
   请逐文件或逐模块说明其作用，尤其是我新增或改动的部分。至少包括：
   - 核心模型模块
   - 数据处理 / 数据抽取 / 预处理模块
   - mask 构造与样本生成模块
   - Flow Matching / Rectified Flow 模块
   - DiT 模块
   - MoE 模块
   - loss 模块（尤其是 spectral loss）
   - 训练脚本
   - 推理 / 采样脚本
   - 评估脚本
   - 配置与参数管理模块
   - 与原始 UniTraj 复用或适配的模块

3. **关键参数与关键设置**
   需要明确写出这些内容：
   - 模型关键参数及含义
   - 数据处理关键参数及含义
   - mask 相关参数
   - Flow Matching 相关参数
   - DiT 相关参数
   - MoE 相关参数
   - spectral loss 相关参数
   - 训练相关参数（batch size、lr、epoch、采样步数等）
   - 推理相关参数
   - 哪些参数最影响结果，哪些参数是默认安全值

4. **如何使用这个修改后的项目**
   请给出从零开始的使用说明，包括：
   - 环境准备
   - 依赖安装
   - 数据准备方式
   - 数据抽取 / 预处理如何运行
   - 训练如何运行
   - 推理 / 采样如何运行
   - 评估如何运行
   - 常见命令示例
   - 输出文件会生成到哪里
   - 各阶段输入输出是什么

5. **修改点总结**
   请明确区分：
   - 原始 UniTraj 的部分
   - 我新增的部分
   - 我修改/重构的部分
   - 与老师参考版本相比保留了什么、改动了什么
   - 当前版本是单阶段还是二阶段，以及原因

6. **代码层面的重点说明**
   请特别解释：
   - 当前 Flow Matching 是如何定义 `x_0`、`x_1` 和路径的
   - 当前模型输入是什么，输出是什么
   - MoE 在哪一层生效
   - spectral loss 约束的对象是什么
   - 训练与推理阶段如何保持一致
   - mask / attention_mask / interval 在当前版本中的作用

7. **潜在问题与注意事项**
   请指出：
   - 当前实现中可能的接口假设
   - 可能的运行前提
   - 容易踩坑的配置
   - 与原始仓库不兼容的地方
   - 后续还值得继续优化的地方
8. **如何使用**

## 输出要求

请直接输出一份**完整的 Markdown 文档正文**，不要只给摘要。

要求：
- 使用清晰的 Markdown 标题层级（# / ## / ###）
- 内容要具体，**必须基于实际代码分析**，不要泛泛而谈
- 尽量引用实际文件名、类名、函数名、脚本名
- 对关键模块写清楚输入、输出、作用、调用关系
- 对重要参数要解释其用途，而不是只列名字
- 文档风格要适合项目交接、代码阅读和论文复现实验
- 不要只写“可能/大概”，要尽量根据代码给出明确结论
- 如果某些地方代码不完整或有歧义，请明确标注“不确定点”

## 输出文件要求

请将最终内容整理为一个 Markdown 文件内容，文件名建议为：

`PROJECT_ARCHITECTURE_AND_USAGE.md`

也就是说，你的最终输出应该是**一份可以直接保存成 `PROJECT_ARCHITECTURE_AND_USAGE.md` 的 Markdown 正文**。

## 分析方式要求

请先阅读整个项目，再输出结果。重点关注：
- 修改后的项目代码,忽略unitraj-flow
- 所有训练 / 推理 / 评估脚本
- 所有数据处理与数据抽取相关模块
- 配置和命令行参数

不要只看 README；必须以代码为准。

如果发现实际实现与文档、注释或命名不一致，请以**代码真实行为**为准，并在文档中明确指出。